registry=https://registry.npmjs.org/

# 本项目使用 Bun 作为主包管理器（bun.lock），禁用 npm 的 package-lock.json 以避免
# 双锁文件冲突。供应链防护通过 package.json overrides + postinstall 检查脚本实现。
# 参考：https://thehackernews.com/2026/04/claude-code-tleaked-via-npm-packaging.html
package-lock=false
